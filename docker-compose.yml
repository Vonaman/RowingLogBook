services:
  db:
    image: postgres:18-alpine
    restart: "no" # Ne redémarre pas automatiquement au démarrage du poste
    container_name: rowing_logbook_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "9876:5432"
    volumes:
      - postgres_data:/var/lib/postgresql # Données persistantes
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql # Init au premier démarrage
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
